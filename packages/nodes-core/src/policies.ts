export const CORE_BOUNDED_JSON_POLICY = Object.freeze({
  key: 'node.json.bounded',
  version: 1,
});

export const CORE_JSONATA_POLICY = Object.freeze({
  key: 'jsonata.restricted',
  version: 1,
});
