// Where and why a run or a step test failed, in words, for summaries outside
// the run page (Home, the editor's Test tab).
export { describeRunFailure, type RunFailure } from './model/run-failure';
export { shortStepError } from './model/step-error-copy';
